name: mojiren
description: もじれん Flutter app for stroke order practice.
publish_to: "none"
version: 1.0.13+14

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  characters: ^1.4.0
  google_mobile_ads: ^7.0.0
  shared_preferences: ^2.5.3
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/kanji/characters.json
